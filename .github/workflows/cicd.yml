name: Backend CI/CD

on:
  workflow_dispatch:
  push:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  TEST_DATABASE_URL: postgres://postgres:password@localhost:5432/aredl_test
  JWT_SECRET: ${{ secrets.JWT_SECRET }}
  BUILD_PROFILE: release

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_PASSWORD: password
          POSTGRES_DB: aredl_test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Installing rust toolchain
      uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable
        override: true
        components: llvm-tools-preview

    - name: Cache cargo for main
      uses: Swatinem/rust-cache@v2

    - name: Run unit tests
      uses: actions-rs/cargo@v1
      with:
        command: test

  generate-code-coverage:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_PASSWORD: password
          POSTGRES_DB: aredl_test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    steps:
      - uses: actions/checkout@v4
      
      - name: Installing rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
          components: llvm-tools-preview

      - name: Cache cargo for main
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Generate coverage report
        run: |
          cargo llvm-cov \
            --workspace \
            --lcov \
            --output-path lcov.info \
            --ignore-filename-regex 'src/schema/|src/scheduled/|src/main\.rs|src/docs\.rs|src/utils/probe/'

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: lcov.info
          fail_ci_if_error: false
          flags: unittests
          token: ${{ secrets.CODECOV_TOKEN }}

  build-api:
    if: github.ref == 'refs/heads/main'
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
        - uses: actions/checkout@v4
        
        - name: Login to GitHub Container Registry
          uses: docker/login-action@v3
          with:
            registry: ghcr.io
            username: ${{ github.actor }}
            password: ${{ secrets.GITHUB_TOKEN }}
            
        - name: Set up Docker Buildx
          uses: docker/setup-buildx-action@v3
        
        - name: Set owner name to lowercase for docker compatibility
          run: |
            echo "OWNER_LC=${OWNER,,}" >>${GITHUB_ENV}
          env:
            OWNER: '${{ github.repository_owner }}'
          
        - name: Build and push API image
          uses: docker/build-push-action@v5
          with:
            context: .
            file: ./Dockerfile
            push: true
            build-args: |
              BUILD_PROFILE=${{ env.BUILD_PROFILE }}
            tags: |
              ghcr.io/${{ env.OWNER_LC }}/aredl_backend_api:latest
              ghcr.io/${{ env.OWNER_LC  }}/aredl_backend_api:${{ github.sha }}
            cache-from: type=registry,ref=ghcr.io/${{ env.OWNER_LC  }}/aredl_backend_api:buildcache
            cache-to: type=registry,ref=ghcr.io/${{ env.OWNER_LC  }}/aredl_backend_api:buildcache,mode=max
  
  pre_deploy_backup:
    if: github.ref == 'refs/heads/main'
    needs: build-api
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/db-backup.yml
    secrets:
      DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
      DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
      GDRIVE_RCLONE_CONFIG: ${{ secrets.GDRIVE_RCLONE_CONFIG }}
  
  deploy:
    if: github.ref == 'refs/heads/main'
    needs: [build-api, pre_deploy_backup]
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/deploy-stack.yml
    with:
      stack_name: aredl_backend_stack
      remote_path: ~/
      docs_api_server: https://api.aredl.net/v2
      api_net: api_net
      db_alias: db_prod
      oauth_return_uri_base: oauth_return_uri_base_prod
      auth_callback_allow_localhost: false
    secrets:
      API_HOST: ${{ secrets.API_HOST }}
      API_PORT: ${{ secrets.API_PORT }}
      API_DB_PORT: ${{ secrets.API_DB_PORT }}
      DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
      DEPLOY_KEY:  ${{ secrets.DEPLOY_KEY }}
      GHCR_USERNAME: ${{ secrets.GHCR_USERNAME }}
      GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}

  apply-backfills-prod:
    if: github.ref == 'refs/heads/main'
    needs: deploy
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/apply-backfills.yml
    with:
      stack_name: aredl_backend_stack
      environment_name: prod
      backfill_repo: ${{ vars.BACKFILL_REPO }}
    secrets:
      API_DB_PORT: ${{ secrets.API_DB_PORT }}
      DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
      DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
      BACKFILL_REPO_TOKEN: ${{ secrets.BACKFILL_REPO_TOKEN }}

  build-api-dev:
    if: github.ref == 'refs/heads/dev'
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
        - uses: actions/checkout@v4
        - name: Login to GitHub Container Registry
          uses: docker/login-action@v3
          with:
            registry: ghcr.io
            username: ${{ github.actor }}
            password: ${{ secrets.GITHUB_TOKEN }}
        - name: Set up Docker Buildx
          uses: docker/setup-buildx-action@v3
        - name: Set owner name to lowercase for docker compatibility
          run: |
            echo "OWNER_LC=${OWNER,,}" >>${GITHUB_ENV}
          env:
            OWNER: '${{ github.repository_owner }}'
        - name: Build and push API dev image
          uses: docker/build-push-action@v5
          with:
            context: .
            file: ./Dockerfile
            push: true
            build-args: |
              BUILD_PROFILE=${{ env.BUILD_PROFILE }}
            tags: |
              ghcr.io/${{ env.OWNER_LC }}/aredl_backend_api:dev
              ghcr.io/${{ env.OWNER_LC }}/aredl_backend_api:${{ github.sha }}
            cache-from: type=registry,ref=ghcr.io/${{ env.OWNER_LC }}/aredl_backend_api:buildcache
            cache-to: type=registry,ref=ghcr.io/${{ env.OWNER_LC }}/aredl_backend_api:buildcache,mode=max

  deploy-dev:
    if: github.ref == 'refs/heads/dev'
    needs: build-api-dev
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/deploy-stack.yml
    with:
      stack_name: aredl_backend_dev_stack
      remote_path: ~/dev
      docs_api_server: https://api.aredl.net/v2dev
      api_net: api_net_dev
      db_alias: db_dev
      oauth_return_uri_base: oauth_return_uri_base_dev
      auth_callback_allow_localhost: true
    secrets:
      API_HOST: ${{ secrets.DEV_API_HOST }}
      API_PORT: ${{ secrets.DEV_API_PORT }}
      API_DB_PORT: ${{ secrets.DEV_API_DB_PORT }}
      DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
      DEPLOY_KEY:  ${{ secrets.DEPLOY_KEY }}
      GHCR_USERNAME: ${{ secrets.GHCR_USERNAME }}
      GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}

  migrate-dev-db:
    if: github.ref == 'refs/heads/dev'
    needs: deploy-dev
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/migrate-dev-db.yml
    secrets:
      DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
      DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}

  apply-backfills-dev:
    if: github.ref == 'refs/heads/dev'
    needs: migrate-dev-db
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/apply-backfills.yml
    with:
      stack_name: aredl_backend_dev_stack
      environment_name: dev
      backfill_repo: ${{ vars.BACKFILL_REPO }}
    secrets:
      API_DB_PORT: ${{ secrets.DEV_API_DB_PORT }}
      DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
      DEPLOY_KEY:  ${{ secrets.DEPLOY_KEY }}
      BACKFILL_REPO_TOKEN: ${{ secrets.BACKFILL_REPO_TOKEN }}
